import { useState } from 'react';import { useWorkspace } from '../data/WorkspaceProvider';import PageHeader from '../components/PageHeader';import Modal from '../components/Modal'
const blank={name:'',foreman:'',phone:'',color:'#80a84c'}
export default function TeamPage(){const {data,add,update}=useWorkspace();const [open,setOpen]=useState(false);const [form,setForm]=useState(blank);function save(e){e.preventDefault();add('crews',form);setOpen(false);setForm(blank)}return <section><PageHeader title="Team & crews" description="Build repeatable crews, assign foremen, and scale without losing control." action={<button className="button primary" onClick={()=>setOpen(true)}>Add crew</button>}/><div className="crew-grid">{data.crews.map(c=><article key={c.id}><div className="crew-color" style={{background:c.color}}/><div className="crew-avatar">{c.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><h2>{c.name}</h2><p>Foreman: <strong>{c.foreman}</strong></p><span>{c.phone||'No phone assigned'}</span><div className="crew-load"><strong>{data.jobs.filter(j=>j.crew_id===c.id&&j.status!=='completed').length}</strong><span>upcoming jobs</span></div><button className="button secondary" onClick={()=>{const name=prompt('Foreman name',c.foreman);if(name!==null)update('crews',c.id,{foreman:name})}}>Edit foreman</button></article>)}</div><article className="panel permissions"><div><p className="eyebrow">ROLE-BASED ACCESS</p><h2>Keep the right information in the right hands</h2><p>Owner sees the full business. Office staff can manage customers and paperwork. Foremen see assigned work, customer contact, signatures, notes, and photos. Crew members can be limited to checklists and uploads.</p></div><div className="role-list"><span><b>Owner</b>Everything</span><span><b>Office</b>CRM + paperwork</span><span><b>Foreman</b>Assigned field work</span><span><b>Crew</b>Tasks + photos</span></div></article><Modal title="Add crew" open={open} onClose={()=>setOpen(false)}><form className="form-grid" onSubmit={save}><label>Crew name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Foreman<input value={form.foreman} onChange={e=>setForm({...form,foreman:e.target.value})}/></label><label>Phone<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Schedule color<input type="color" value={form.color} onChange={e=>setForm({...form,color:e.target.value})}/></label><div className="form-actions wide"><button type="button" className="button secondary" onClick={()=>setOpen(false)}>Cancel</button><button className="button primary">Add crew</button></div></form></Modal></section>}
