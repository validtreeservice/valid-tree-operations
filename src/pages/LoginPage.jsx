import { useState } from 'react';import { Navigate } from 'react-router-dom';import { useAuth } from '../components/AuthProvider'
export default function LoginPage(){const {user,signIn,demo}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');if(user)return <Navigate to="/" replace/>;async function submit(e){e.preventDefault();const {error}=await signIn(email,password);if(error)setError(error.message)}return <main className="login"><section className="login-visual"><img src="/valid-tree-logo.png"/><div><p className="eyebrow light">VALID TREE SERVICE LLC</p><h1>Your entire operation.<br/>One place.</h1><p>Customers, crews, contracts, signatures, job photos, invoices, and intelligent follow-up—built for the field.</p></div></section><section className="login-panel"><form onSubmit={submit}><p className="eyebrow">PRIVATE OPERATIONS PORTAL</p><h2>Welcome back</h2><p className="muted">Sign in to manage today’s work.</p>{error?<p className="form-message error">{error}</p>:null}<label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@validtreeservice.com" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="button primary wide-button">Sign in securely</button><div className="or"><span>or preview now</span></div><button type="button" className="button secondary wide-button" onClick={demo}>Open demo workspace</button><small>Demo mode stores sample data only in this browser.</small></form></section></main>}
