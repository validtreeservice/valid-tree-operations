import { useWorkspace } from '../data/WorkspaceProvider';import PageHeader from '../components/PageHeader';import StatusBadge from '../components/StatusBadge'
export default function SchedulePage(){const {data,customer,crew}=useWorkspace();const days=[...new Set(data.jobs.map(x=>x.date))].sort();return <section><PageHeader title="Schedule" description="See every crew’s workload and prevent double-booking before it happens."/><div className="schedule-legend">{data.crews.map(c=><span key={c.id}><i style={{background:c.color}}/>{c.name}</span>)}</div><div className="schedule">{days.map(d=><div className="schedule-day" key={d}><div className="schedule-date"><strong>{new Date(d+'T12:00').toLocaleDateString('en-US',{weekday:'short'})}</strong><b>{new Date(d+'T12:00').getDate()}</b><span>{new Date(d+'T12:00').toLocaleDateString('en-US',{month:'short'})}</span></div><div className="schedule-jobs">{data.jobs.filter(j=>j.date===d).map(j=><article key={j.id} style={{borderLeftColor:crew(j.crew_id)?.color}}><div><strong>{j.start_time} · {j.title}</strong><span>{customer(j.customer_id)?.full_name} — {j.address}</span></div><div><small>{crew(j.crew_id)?.name}</small><StatusBadge value={j.status}/></div></article>)}</div></div>)}</div></section>}
