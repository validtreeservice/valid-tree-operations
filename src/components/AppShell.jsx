import { NavLink, Outlet, useLocation } from 'react-router-dom'
import { useState } from 'react'
import { useAuth } from './AuthProvider'
const links=[['/','Dashboard','▦'],['/ai','AI Assistant','✦'],['/customers','Customers','♙'],['/estimates','Estimates','⌁'],['/contracts','Contracts','▤'],['/jobs','Jobs','♣'],['/schedule','Schedule','▦'],['/invoices','Invoices','$'],['/tablet','Tablet Mode','▣'],['/team','Team & Crews','♟'],['/settings','Settings','⚙']]
export default function AppShell(){const [open,setOpen]=useState(false);const {signOut,isDemo}=useAuth();const loc=useLocation();return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><img src="/valid-tree-logo.png"/><div><strong>VALID TREE</strong><span>Operations</span></div></div><nav>{links.map(([to,label,icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}><i>{icon}</i>{label}</NavLink>)}</nav><div className="sidebar-foot"><span className={isDemo?'mode demo':'mode'}>{isDemo?'Demo workspace':'Live workspace'}</span><button onClick={signOut}>Sign out</button></div></aside><div className="main"><header className="topbar"><button className="menu" onClick={()=>setOpen(!open)}>☰</button><div><strong>{links.find(([p])=>p===loc.pathname)?.[1]||'Valid Tree Service'}</strong><span>Houston operations center</span></div><div className="top-actions"><NavLink className="quick" to="/ai">✦ Ask AI</NavLink><div className="avatar">VT</div></div></header><main className="content"><Outlet/></main></div>{open?<button className="scrim" onClick={()=>setOpen(false)}/>:null}</div>}
